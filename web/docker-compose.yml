version: "3.8"
services:
  web:
    build: .
    networks:
      - proxy
    labels:
      - "traefik.http.routers.web.rule=Host(`pi.valfur.fr`)"
      - "traefik.enable=true"
    restart: unless-stopped

networks:
  proxy:
    external: true
